# 异常兜底状态机

生成一张状态流转图，1200×900px，白色背景。

顶部标题：异常兜底状态机（铁律 4 的可视化）

画面顶部一个椭圆（浅灰填充，黑色文字）：用户触发功能操作

从椭圆向下箭头进入一个矩形状态框（黄色 #ecc94b 填充，黑色粗体文字）：
加载态 (Loading)
副标题：骨架屏 / 加载动画

从加载态向下分出 3 条箭头，分别到达三个不同颜色的矩形状态框：

左分支：
- 成功态 (Happy)——绿色 #38a169 填充，白色文字
- 副标题：渲染正常业务数据

中分支：
- 空数据态 (Empty)——灰色 #a0aec0 填充，白色文字
- 副标题："暂无数据"，引导操作

右分支：
- 错误态 (Error)——红色 #e53e3e 填充，白色文字
- 副标题：网络错误 / 超时 / 权限不足 / 服务异常
- 从错误态继续向下箭头指向一个椭圆：
  重试机制（手动/自动）——橙色 #dd6b20 填充，白色文字

画面底部灰色小字说明：所有涉及网络请求、用户输入的组件必须实现以上三种状态的显式处理，否则视为未完成。

箭头均为灰色实线，线宽 2px，箭头上方标注触发条件（如"请求成功"、"数据为空"、"请求失败"）。